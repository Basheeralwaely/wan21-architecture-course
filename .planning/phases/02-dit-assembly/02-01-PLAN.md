---
phase: 02-dit-assembly
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Course/NB-06-dit-block.ipynb
  - Course/NB-07-patchify-unpatchify.ipynb
autonomous: true
requirements: [DIT-12, DIT-13, DIT-14, DIT-15]

must_haves:
  truths:
    - "Reader can open NB-06 and see the DiTBlock's six sub-modules wired in __init__, then trace the forward pass line-by-line with back-references to NB-01 (modulate), NB-04 (attention), NB-05 (adaLN-Zero)"
    - "Reader can see per-sub-module parameter counts (q, k, v, o, ffn.0, ffn.2) with percentages, and understand why these are LoRA targets — then see the 30-block scaling"
    - "Reader can open NB-07 and observe Conv3d patchify: (B,48,F,H,W) -> (B,F*(H//2)*(W//2),1536), then einops unpatchify reversing it back to (B,16,F,H,W)"
    - "Reader can see the Head module applying 2-parameter adaLN (shift+scale) using raw time embedding t, not the 6-chunk t_mod"
    - "All cells in both notebooks run on CPU in under 5 seconds total with all assertions passing"
  artifacts:
    - path: "Course/NB-06-dit-block.ipynb"
      provides: "DiT Block composition walkthrough + LoRA parameter analysis"
      contains: "DiTBlock"
    - path: "Course/NB-07-patchify-unpatchify.ipynb"
      provides: "Patchify/unpatchify round-trip + Head module walkthrough"
      contains: "patchify"
  key_links:
    - from: "Course/NB-06-dit-block.ipynb"
      to: "diffsynth/models/wan_video_dit.py"
      via: "importlib setup cell imports DiTBlock, GateModule, modulate, precompute_freqs_cis_3d"
      pattern: "from diffsynth.models.wan_video_dit import.*DiTBlock"
    - from: "Course/NB-07-patchify-unpatchify.ipynb"
      to: "diffsynth/models/wan_video_dit.py"
      via: "importlib setup cell imports WanModel, Head"
      pattern: "from diffsynth.models.wan_video_dit import.*WanModel.*Head"
---

<objective>
Create NB-06 (DiT Block composition) and NB-07 (Patchify/Unpatchify/Head) Jupyter notebooks in Course/.

Purpose: These two notebooks assemble Phase 1 primitives into composed systems. NB-06 shows how self-attention, cross-attention, FFN, and adaLN conditioning compose into a single DiT Block, with LoRA target analysis. NB-07 shows how video tensors convert to/from token sequences via Conv3d patchify and einops unpatchify, plus the Head output projection.

Output: Two runnable .ipynb files following the Phase 1 notebook template (STD-01 through STD-07).
</objective>

<execution_context>
@/home/basheer/.claude/get-shit-done/workflows/execute-plan.md
@/home/basheer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dit-assembly/02-CONTEXT.md
@.planning/phases/02-dit-assembly/02-RESEARCH.md
@.planning/phases/02-dit-assembly/02-PATTERNS.md

<interfaces>
<!-- Source code the executor needs. From diffsynth/models/wan_video_dit.py -->

DiTBlock (line 197-231):
- __init__(self, has_image_input, dim, ffn_dim, num_heads)
- forward(self, x, context, t_mod, freqs) -> x
- Sub-modules: self_attn (SelfAttention), cross_attn (CrossAttention), norm1/2/3 (LayerNorm), ffn (Sequential), modulation (nn.Parameter [1,6,dim]), gate (GateModule)

GateModule (line 190-195):
- forward(self, x, gate, residual) -> x + gate * residual

Head (line 254-270):
- __init__(self, dim, out_dim, patch_size, eps)
- forward(self, x, t) -> out  [NOTE: receives 't' not 't_mod']
- Sub-modules: norm (LayerNorm no affine), head (Linear dim -> out_dim*prod(patch_size)), modulation (nn.Parameter [1,2,dim])

WanModel.patchify (line 340-350):
- patch_embedding = Conv3d(in_dim=48, dim=1536, kernel=(1,2,2), stride=(1,2,2))
- rearrange(px, 'b c f h w -> b (f h w) c')

WanModel.unpatchify (line 352-357):
- rearrange(x, 'b (f h w) (x y z c) -> b c (f x) (h y) (w z)', f=f, h=h, w=w, x=ps[0], y=ps[1], z=ps[2])

precompute_freqs_cis_3d(dim) -> (f_freqs, h_freqs, w_freqs):
- f_freqs: [1024, 22], h_freqs: [1024, 21], w_freqs: [1024, 21]
- Assembly: torch.cat([f[:F].expand, h[:H].expand, w[:W].expand], dim=-1).reshape(F*H*W, 1, -1)
- Result shape: [seq_len, 1, head_dim//2] where head_dim//2 = 22+21+21 = 64

modulate(x, shift, scale) -> x * (1 + scale) + shift
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NB-06 DiT Block notebook</name>
  <files>Course/NB-06-dit-block.ipynb</files>
  <read_first>
    - Course/NB-05-adaln-zero-modulation.ipynb (primary analog — copy template structure, setup cell, exercise format)
    - diffsynth/models/wan_video_dit.py (lines 190-231 — GateModule, DiTBlock __init__ and forward)
    - .planning/phases/02-dit-assembly/02-PATTERNS.md (complete cell-by-cell pattern with code)
    - .planning/phases/02-dit-assembly/02-RESEARCH.md (parameter count table, pitfalls, timing budget)
  </read_first>
  <action>
Create `Course/NB-06-dit-block.ipynb` as a Jupyter notebook (.ipynb JSON) following the Phase 1 template.

Target cell count: ~15 cells. Use nbformat or direct JSON (same approach as Phase 1 plan execution).

**Cell structure (per D-01 bottom-up buildup, D-02 ASCII diagram):**

Cell 0 (markdown): Title, Learning Objectives, Prerequisites, Concept Map.
- Title: "# NB-06: The DiT Block -- Assembling Self-Attention, Cross-Attention, and FFN"
- Prerequisites list: NB-01 (modulate), NB-04 (SelfAttention, CrossAttention), NB-05 (adaLN-Zero, GateModule)
- Include the ASCII block diagram from 02-PATTERNS.md showing: input -> adaLN -> self-attn -> cross-attn -> FFN -> output with residual connections (per D-02)

Cell 1 (code): Setup cell — copy VERBATIM from NB-05 cell 1 (the 6-level upward path search importlib pattern). Change only the final imports to:
```python
from diffsynth.models.wan_video_dit import (
    DiTBlock, GateModule, modulate, precompute_freqs_cis_3d
)
import torch
import torch.nn as nn
```

Cell 2 (markdown): "## 1. DiTBlock.__init__ -- Sub-Module Wiring" prose explaining that DiTBlock assembles six sub-modules from Phase 1 primitives. Reference source: `diffsynth/models/wan_video_dit.py, lines 197-213`.

Cell 3 (code): DiTBlock instantiation + sub-module inventory. Use `dim=1536, num_heads=12, ffn_dim=8960, has_image_input=True`. Print each sub-module with parameter counts (exact code in 02-PATTERNS.md "DiTBlock.__init__ sub-module inventory pattern"). Include inline shape annotations (STD-02).

Cell 4 (markdown): "## 2. DiTBlock.forward() -- Line-by-Line Walkthrough" prose explaining the four stages: (1) six-parameter chunk from modulation+t_mod, (2) self-attention branch with adaLN + gated residual, (3) cross-attention branch with simple residual, (4) FFN branch with adaLN + gated residual. Use back-references: "(recall NB-05: adaLN-Zero)", "(recall NB-04: SelfAttention)", "(recall NB-01: modulate)".

Cell 5 (markdown): "### Constructing the 3D Frequency Tensor" prose explaining that DiTBlock.forward needs full 3-band freqs (NOT the simplified single-band from NB-04). Reference WanModel.forward lines 381-385.

Cell 6 (code): 3D freqs assembly — use the EXACT pattern from 02-PATTERNS.md "3D freqs assembly pattern for DiTBlock":
- `dim, num_heads, ffn_dim = 1536, 12, 8960`
- `head_dim = dim // num_heads  # 128`
- `f_freqs, h_freqs, w_freqs = precompute_freqs_cis_3d(head_dim)`
- `F, H, W = 4, 4, 4`
- `freqs = torch.cat([f_freqs[:F]..., h_freqs[:H]..., w_freqs[:W]...], dim=-1).reshape(F*H*W, 1, -1)`
- `assert freqs.shape == torch.Size([F * H * W, 1, head_dim // 2])`
Add comment: `# Source: diffsynth/models/wan_video_dit.py, lines 381-385`

Cell 7 (code): DiTBlock forward pass — use EXACT pattern from 02-PATTERNS.md "DiTBlock forward pass verification pattern":
- `block.eval()`, `B, S = 1, F * H * W`
- `x = torch.randn(B, S, dim)`, `context = torch.randn(B, 277, dim)`, `t_mod = torch.randn(B, 6, dim)`
- `with torch.no_grad(): out = block(x, context, t_mod, freqs)`
- `assert out.shape == torch.Size([B, S, dim])`
Add comment: `# Source: diffsynth/models/wan_video_dit.py, lines 215-231`

Cell 8 (markdown): "### Annotated Forward Pass" prose explaining each line of the forward method. Use the annotated version from 02-PATTERNS.md "DiTBlock forward annotated pattern" — show the four stages with line-level comments.

Cell 9 (code): The annotated forward pass code showing the four stages with inline comments:
```python
# 1. Extract six modulation parameters (recall NB-05: adaLN-Zero)
shift_msa, scale_msa, gate_msa, shift_mlp, scale_mlp, gate_mlp = (
    block.modulation.to(dtype=t_mod.dtype, device=t_mod.device) + t_mod
).chunk(6, dim=1)
# 2. Self-attention branch (recall NB-04: SelfAttention, NB-01: modulate)
# 3. Cross-attention branch -- no adaLN, simple residual
# 4. FFN branch (recall NB-01: modulate)
```
This cell demonstrates each step individually with print statements showing intermediate shapes.

Cell 10 (markdown): "## 3. Parameter Count Breakdown and LoRA Targets" prose per D-03 — counts + rationale. Explain WHY q, k, v, o, ffn.0, ffn.2 are LoRA targets (task-specific representations in attention; high-dimensional feature transform in FFN). No LoRA math or rank decomposition.

Cell 11 (code): Per-block LoRA target parameter counts — use EXACT code from 02-PATTERNS.md "LoRA parameter scaling pattern". Print each target with count and percentage. Show `lora_total_per_block = 46,422,272` which is `90.7%` of block total `51,163,904`. Note that `cross_attn.k_img` and `cross_attn.v_img` are NOT LoRA targets (verified from `train_lora_cached.py` line 292 default `--lora_target_modules "q,k,v,o,ffn.0,ffn.2"`).

Cell 12 (code): Full-model LoRA scaling per D-04 — multiply per-block counts by `num_layers=30`. Print `30-block LoRA total: 1,392,668,160`.

Cell 13 (markdown): Summary — key takeaways and source references table per 02-PATTERNS.md "Summary markdown pattern". Include: DiTBlock composition, freqs construction requirement, LoRA targets (90.7% per block).

Cell 14 (markdown): Exercises — 3 exercises per 02-PATTERNS.md "Exercise pattern":
1. Remove cross-attention (has_image_input=False, smaller context)
2. Inspect gate values (manually compute gate_msa, check near-zero)
3. Scale LoRA to different ranks (compute compression ratio for rank r=16)

**Standards compliance:**
- STD-01: Prerequisites cell lists NB-01, NB-04, NB-05
- STD-02: Inline shape annotations on every tensor operation
- STD-03: All cells run on CPU in under 5 seconds (verified: DiTBlock forward at S=64 takes 0.020s)
- STD-04: assert statements after every key operation
- STD-05: Markdown cell before every code cell
- STD-06: Source line citations on first use of each symbol
- STD-07: Real diffsynth classes imported via importlib
  </action>
  <verify>
    <automated>cd /home/basheer/Signapse/Codes/stand_alone_Wan_lora_training && python3 -c "
import json
with open('Course/NB-06-dit-block.ipynb') as f:
    nb = json.load(f)
cells = nb['cells']
src = ''.join([''.join(c['source']) for c in cells])
checks = [
    ('cell_count', 12 <= len(cells) <= 18),
    ('has_DiTBlock', 'DiTBlock' in src),
    ('has_freqs_assembly', 'precompute_freqs_cis_3d' in src),
    ('has_shape_assert', 'assert out.shape' in src or 'assert freqs.shape' in src),
    ('has_lora_section', 'LoRA' in src),
    ('has_param_count', 'numel()' in src),
    ('has_ascii_diagram', 'SelfAttention' in src and 'CrossAttention' in src and 'FFN' in src),
    ('has_exercises', 'Exercise' in src),
    ('has_summary', 'Summary' in src),
    ('has_source_citation', 'Source: diffsynth/models/wan_video_dit.py' in src),
    ('has_prerequisites', 'NB-01' in src and 'NB-04' in src and 'NB-05' in src),
    ('has_importlib', 'importlib.util' in src),
]
for name, result in checks:
    status = 'PASS' if result else 'FAIL'
    print(f'{status}: {name}')
all_pass = all(r for _, r in checks)
print(f'Overall: {\"PASS\" if all_pass else \"FAIL\"} ({sum(r for _,r in checks)}/{len(checks)})')
exit(0 if all_pass else 1)
" && jupyter nbconvert --to notebook --execute --ExecutePreprocessor.timeout=30 Course/NB-06-dit-block.ipynb --output /tmp/nb06-test.ipynb 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Course/NB-06-dit-block.ipynb exists and is valid JSON
    - Notebook contains 12-18 cells (markdown + code interleaved)
    - Cell 0 markdown contains "# NB-06" and "Learning Objectives" and "Prerequisites" and "NB-01" and "NB-04" and "NB-05"
    - Cell 0 markdown contains ASCII block diagram with "SelfAttention" and "CrossAttention" and "FFN" and "gate"
    - Setup cell contains "importlib.util" and "DiTBlock" and "precompute_freqs_cis_3d"
    - Code cells contain "precompute_freqs_cis_3d(head_dim)" for 3D freqs assembly
    - Code cells contain "assert freqs.shape == torch.Size" and "assert out.shape == torch.Size"
    - Code cells contain "block.self_attn.q.parameters()" or equivalent per-sub-module counting
    - Markdown contains "LoRA" and code contains "lora_target" or equivalent naming
    - Code cell prints 30-block total: contains "num_layers" or "30" multiplied with per-block count
    - Summary cell contains "Source References" table with "DiTBlock.__init__" and line numbers
    - Exercises cell contains "Exercise 1", "Exercise 2", "Exercise 3"
    - Source citations present: "Source: diffsynth/models/wan_video_dit.py" appears in code comments
    - jupyter nbconvert --execute completes without error (all assertions pass, CPU execution under 30s)
  </acceptance_criteria>
  <done>NB-06 notebook exists with DiTBlock composition walkthrough (D-01 bottom-up), ASCII diagram (D-02), per-sub-module + full-model LoRA parameter counts (D-03, D-04), all STD-01 through STD-07 standards, and executes on CPU with all assertions passing.</done>
</task>

<task type="auto">
  <name>Task 2: Create NB-07 Patchify/Unpatchify/Head notebook</name>
  <files>Course/NB-07-patchify-unpatchify.ipynb</files>
  <read_first>
    - Course/NB-01-rmsnorm-sinusoidal-modulate.ipynb (multi-concept analog — 3 concepts in one notebook)
    - diffsynth/models/wan_video_dit.py (lines 254-270 Head, lines 340-357 patchify/unpatchify)
    - .planning/phases/02-dit-assembly/02-PATTERNS.md (complete cell patterns for NB-07)
    - .planning/phases/02-dit-assembly/02-RESEARCH.md (Pattern 2 patchify round-trip, Head code example, Pitfall 5 unpatchify axis order)
  </read_first>
  <action>
Create `Course/NB-07-patchify-unpatchify.ipynb` as a Jupyter notebook (.ipynb JSON) following the Phase 1 template.

Target cell count: ~13 cells. Three concepts (patchify, unpatchify, Head) mirroring NB-01's three-concept structure.

**Cell structure (per D-05 ASCII diagrams, D-06 Head module inclusion):**

Cell 0 (markdown): Title, Learning Objectives, Prerequisites, Concept Map.
- Title: "# NB-07: Patchify, Unpatchify, and the Head Module"
- Prerequisites: NB-01 (modulate -- Head uses it), NB-06 (DiTBlock -- patchify produces tokens for DiT blocks)
- Include the ASCII spatial-to-sequence diagram from 02-PATTERNS.md showing: [B,48,F,H,W] -> Conv3d(stride=1,2,2) -> [B,F*(H//2)*(W//2),1536] (per D-05)

Cell 1 (code): Setup cell -- copy VERBATIM from NB-05 cell 1 (6-level upward path search). Change final imports to:
```python
from diffsynth.models.wan_video_dit import WanModel, Head
import torch
import torch.nn as nn
import math
from einops import rearrange
```

Cell 2 (markdown): "## 1. Patchify -- Video to Token Sequence" prose explaining Conv3d with kernel (1,2,2) and stride (1,2,2). Reference: `diffsynth/models/wan_video_dit.py, lines 340-350`. Explain that patch_size[0]=1 means temporal dimension is preserved -- each latent frame becomes H//2 * W//2 tokens.

Cell 3 (code): Patchify demo -- use EXACT code from 02-PATTERNS.md "Patchify pattern":
- `patch_size = (1, 2, 2)`, `in_dim, dim = 48, 1536`
- `patch_embedding = nn.Conv3d(in_dim, dim, kernel_size=patch_size, stride=patch_size)`
- Print weight shape: `(1536, 48, 1, 2, 2)` and bias shape: `(1536,)`
- `B, C, F, H, W = 1, 48, 4, 8, 8`
- `x = torch.randn(B, C, F, H, W)  # [1, 48, 4, 8, 8]`
- `px = patch_embedding(x)  # [1, 1536, 4, 4, 4]`
- `x_seq = rearrange(px, 'b c f h w -> b (f h w) c')  # [1, 64, 1536]`
- `assert x_seq.shape == torch.Size([B, f * h * w, dim])`
Add source citation: `# Source: diffsynth/models/wan_video_dit.py, lines 340-350`
Add inline shape annotations on every line (STD-02).

Cell 4 (markdown): "## 2. Unpatchify -- Token Sequence Back to Video" prose explaining the einops rearrange reversal. Reference: `diffsynth/models/wan_video_dit.py, lines 352-357`. EXPLICITLY note the axis label order `(x y z c)` meaning patch_t, patch_h, patch_w, channel -- warn about Pitfall 5 (axis swap produces wrong spatial layout).

Cell 5 (code): Unpatchify demo -- use EXACT code from 02-PATTERNS.md "Unpatchify pattern":
- `out_dim = 16`
- `out_dim_full = out_dim * math.prod(patch_size)  # 16 * 1*2*2 = 64`
- `head_output = torch.randn(B, f * h * w, out_dim_full)  # [1, 64, 64]`
- Copy EXACT rearrange string from WanModel.unpatchify (line 353-356):
  `x_video = rearrange(head_output, 'b (f h w) (x y z c) -> b c (f x) (h y) (w z)', f=f, h=h, w=w, x=patch_size[0], y=patch_size[1], z=patch_size[2])`
- `assert x_video.shape == torch.Size([B, out_dim, F, H, W])`
- Print round-trip: `f"Round-trip: {x.shape} -> patchify({x_seq.shape}) -> unpatchify({x_video.shape})"`
Add source citation: `# Source: diffsynth/models/wan_video_dit.py, lines 352-357`

Cell 6 (markdown): "### Patchify/Unpatchify Round-Trip Verification" prose noting that patchify and unpatchify are NOT mathematical inverses (patchify uses learned Conv3d weights while unpatchify is a deterministic reshape), but the SHAPE round-trip is exact: `(B,C,F,H,W) -> (B,S,dim) -> (B,out_dim,F,H,W)`.

Cell 7 (code): Shape round-trip verification cell. Assert input spatial dims match output spatial dims:
```python
assert x.shape[2:] == x_video.shape[2:], "Spatial dimensions must match"
print(f"Input spatial:  {x.shape[2:]}  (F={F}, H={H}, W={W})")
print(f"Output spatial: {x_video.shape[2:]}  (F={F}, H={H}, W={W})")
print(f"Channel change: {x.shape[1]} (in_dim=48) -> {x_video.shape[1]} (out_dim=16)")
```

Cell 8 (markdown): "## 3. Head Module -- Output Projection with adaLN" prose per D-06 explaining that Head is the final projection applied after all DiT blocks, before unpatchify. EXPLICITLY note: "Head receives `t` (the 1536-dim time embedding before the 6-chunk projection). It applies its own 2-parameter adaLN (shift + scale for output normalization), NOT the 6-parameter modulation from DiTBlock." Reference: `diffsynth/models/wan_video_dit.py, lines 254-270`.

Cell 9 (code): Head demo -- use EXACT code from 02-PATTERNS.md "Head module pattern":
- `dim, out_dim, patch_size = 1536, 16, (1, 2, 2)`
- `head = Head(dim=dim, out_dim=out_dim, patch_size=patch_size, eps=1e-6)`
- Print sub-modules: norm (0 params, no affine), head linear (dim -> out_dim*prod(patch_size) = 1536 -> 64), modulation ([1,2,dim])
- `B, S = 1, 64`
- `x = torch.randn(B, S, dim)  # [B, seq, dim]`
- `t = torch.randn(B, dim)  # [B, dim] -- time embedding BEFORE time_projection`
- `head.eval()`, `with torch.no_grad(): out = head(x, t)`
- `assert out.shape == torch.Size([B, S, out_dim * math.prod(patch_size)])`
Add source citation and inline shape annotations.

Cell 10 (markdown): "### Head's Place in the WanModel Pipeline" prose connecting Head to the full pipeline: patchify -> N x DiTBlock -> Head -> unpatchify. The Head output `[B, S, 64]` is exactly what unpatchify expects as input (64 = out_dim * prod(patch_size) = 16 * 1 * 2 * 2).

Cell 11 (markdown): Summary -- key takeaways and source references per 02-PATTERNS.md. Three takeaways covering patchify (temporal preservation), unpatchify (exact einops string), and Head (2-parameter adaLN with raw t).

Cell 12 (markdown): Exercises -- 3 exercises:
1. Change patch size to (1,4,4) -- what happens to sequence length and Conv3d weight shape?
2. Feed Head output directly into unpatchify -- verify the round-trip produces correct spatial dims
3. Compare patchify Conv3d parameter count to a single DiTBlock -- how much of the total model is patchify?

**Standards compliance:** Same as Task 1 (STD-01 through STD-07).
  </action>
  <verify>
    <automated>cd /home/basheer/Signapse/Codes/stand_alone_Wan_lora_training && python3 -c "
import json
with open('Course/NB-07-patchify-unpatchify.ipynb') as f:
    nb = json.load(f)
cells = nb['cells']
src = ''.join([''.join(c['source']) for c in cells])
checks = [
    ('cell_count', 10 <= len(cells) <= 16),
    ('has_patchify', 'patchify' in src.lower() or 'patch_embedding' in src),
    ('has_unpatchify', 'unpatchify' in src.lower()),
    ('has_Head', 'Head(' in src),
    ('has_Conv3d', 'Conv3d' in src),
    ('has_rearrange', 'rearrange' in src),
    ('has_round_trip_assert', 'assert x_video.shape' in src),
    ('has_ascii_diagram', 'stride' in src.lower() and ('H//2' in src or 'h=4' in src)),
    ('has_exercises', 'Exercise' in src),
    ('has_summary', 'Summary' in src),
    ('has_source_citation', 'Source: diffsynth/models/wan_video_dit.py' in src),
    ('has_prerequisites', 'NB-01' in src and 'NB-06' in src),
    ('has_importlib', 'importlib.util' in src),
    ('has_head_t_note', 't_mod' in src or 'time embedding' in src.lower()),
]
for name, result in checks:
    status = 'PASS' if result else 'FAIL'
    print(f'{status}: {name}')
all_pass = all(r for _, r in checks)
print(f'Overall: {\"PASS\" if all_pass else \"FAIL\"} ({sum(r for _,r in checks)}/{len(checks)})')
exit(0 if all_pass else 1)
" && jupyter nbconvert --to notebook --execute --ExecutePreprocessor.timeout=30 Course/NB-07-patchify-unpatchify.ipynb --output /tmp/nb07-test.ipynb 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Course/NB-07-patchify-unpatchify.ipynb exists and is valid JSON
    - Notebook contains 10-16 cells (markdown + code interleaved)
    - Cell 0 markdown contains "# NB-07" and "Learning Objectives" and "Prerequisites" and "NB-01" and "NB-06"
    - Cell 0 markdown contains ASCII spatial-to-sequence diagram with "Conv3d" and "stride" and "(1,2,2)"
    - Setup cell contains "importlib.util" and "WanModel" and "Head" and "rearrange"
    - Patchify code cell contains "nn.Conv3d(in_dim, dim, kernel_size=patch_size, stride=patch_size)"
    - Patchify code cell contains "rearrange(px, 'b c f h w -> b (f h w) c')"
    - Unpatchify code cell contains "rearrange" with "'b (f h w) (x y z c) -> b c (f x) (h y) (w z)'"
    - Unpatchify code cell contains "assert x_video.shape == torch.Size([B, out_dim, F, H, W])"
    - Head code cell contains "Head(dim=dim, out_dim=out_dim, patch_size=patch_size" and "head(x, t)" (not head(x, t_mod))
    - Head section markdown or code contains note about "t" vs "t_mod" distinction
    - Summary cell contains "Source References" table with "WanModel.patchify" and "Head" entries
    - Exercises cell contains "Exercise 1", "Exercise 2", "Exercise 3"
    - jupyter nbconvert --execute completes without error (all assertions pass, CPU execution under 30s)
  </acceptance_criteria>
  <done>NB-07 notebook exists with patchify Conv3d demo (D-05 ASCII diagram), unpatchify einops round-trip, Head module with t vs t_mod clarification (D-06), all STD-01 through STD-07 standards, and executes on CPU with all assertions passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Notebooks are read-only educational files. No network calls, no user data, no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I (Information Disclosure) | Notebook code cells | accept | Notebooks contain only dummy tensors and public architecture walkthrough code. No secrets or PII. |
</threat_model>

<verification>
1. Both .ipynb files exist in Course/ and are valid JSON
2. Both notebooks execute end-to-end via `jupyter nbconvert --execute` with all assertions passing
3. Total execution time for each notebook is under 5 seconds on CPU (STD-03)
4. Both notebooks follow Phase 1 template: Title+LO+Prerequisites+ConceptMap, Setup, Prose-Code sections, Summary, Exercises
5. All source citations reference correct line numbers in diffsynth/models/wan_video_dit.py
</verification>

<success_criteria>
- NB-06 demonstrates DiTBlock composition (DIT-12) with bottom-up buildup (D-01) and ASCII diagram (D-02)
- NB-06 shows LoRA parameter counts per sub-module with percentages and WHY rationale (DIT-13, D-03) at both per-block and 30-block scale (D-04)
- NB-07 demonstrates patchify Conv3d video-to-token conversion (DIT-14) with ASCII spatial-to-sequence diagram (D-05)
- NB-07 demonstrates unpatchify token-to-video reconstruction (DIT-15) with shape round-trip assertion
- NB-07 includes Head module output projection (D-06)
- Both notebooks pass all 7 STD standards and execute on CPU under 5 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/02-dit-assembly/02-01-SUMMARY.md`
</output>
